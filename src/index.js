import validator from './validator.js';

console.log(validator);
function validaCampoNome(){
    const campoNome = 
    document.getElementById("nomedotitular");

    campoNome.classList.remove(ïnvalid);
    let valid = true;
    console.log('')
}
